# Notebooks

- `self_play_training_colab.ipynb`: standalone Colab notebook for self-play training with Colab Secrets (`HF_TOKEN`, optional `WANDB_API_KEY`, optional `OPENAI_API_KEY`), repo bootstrap, Colab-safe config generation, and the same Python self-play runner used by the CLI.
